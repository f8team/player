# @f8/player-preset-web

Single entry point to assemble the plugin list shared by **f8-ui**, **f8-dash-ui**, and **f8-pro-ui**.

Use `createF8WebPlayerPlugins()` so fullscreen / PiP / HLS auth / thumbnails wiring stay aligned across surfaces.
