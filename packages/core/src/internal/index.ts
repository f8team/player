/**
 * `@f8team/reel-core/internal` — escape hatches for adapters and tests.
 *
 * Anything exported from this subpath is `@internal` and may change without a
 * semver bump. Adapters that pin to internals must run their own integration
 * tests against new core releases.
 */

export {};
